{
  app {
    http-server {
      port: 9000
    }
    services {
      connector-hub-connections-url: ${SERVICES_CONNECTOR_HUB_CONNECTIONS_URL}
      connector-hub-metadata-url: ${SERVICES_CONNECTOR_HUB_METADATA_URL}
    }
  }
}
